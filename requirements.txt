# Runtime dependencies. Versions are lower bounds, not pins: the code uses only
# long-stable APIs. The published figures were produced with numpy 1.23.1 and
# matplotlib 3.7.2 on Python 3.10.
#
# numpy >= 1.17 is required because the simulation uses the Generator API
# (numpy.random.default_rng); the legacy RandomState API produces different
# numbers and will not reproduce the published arrays.
numpy>=1.17
matplotlib>=3.3
